version: "3"

includes:
  lang:
    taskfile: ./language
    dir: ./language

tasks:
  default:
    desc: default action - lists the tasks (you're looking at it)
    silent: true
    cmds:
      - task --list
